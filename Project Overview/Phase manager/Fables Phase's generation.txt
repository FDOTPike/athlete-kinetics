# Athlete Kinetics — Phase Manager (rev 2, 2026-06-11)

Living status document. Revised after completing Steps 1-2; revise again
after each phase. Verification gate for every step: `npm run verify:all`
green + a step-specific check listed below.

## Phase 6 — Make it run on a device

[DONE] Step 1 — Git safety net.
  Commit b62a48b (46 files, 8,597 lines), then e303181 (native scaffold).
  .gitattributes normalizes line endings; local editor settings ignored.
  OPEN DECISION: GitHub remote (public unlocks free CI + F-Droid path).

[DONE*] Step 2 — Native projects scaffolded (RN 0.81.6, monorepo-adapted).
  android/ + ios/ live in apps/mobile; gradle paths point at the hoisted
  workspace node_modules; apps/mobile/package.json added for autolinking.
  Verified headlessly: Metro PRODUCTION BUNDLE builds (1.06 MB — schema SQL
  inlined through babel inline-import, policy engine + all screens present)
  and `react-native config` autolinks op-sqlite's Android native source.
  *Remaining: the Gradle compile itself. BLOCKED on toolchain (below).

[BLOCKED — USER DECISION] Step 2b (new) — Android toolchain on this PC.
  No JDK, no Android SDK present. winget IS available. Options:
    (a) Full Android Studio (~3 GB; GUI, emulator manager — easiest)
    (b) Lean CLI-only: winget Microsoft.OpenJDK.17 + Android cmdline-tools
        + sdkmanager packages (~1.5 GB, scriptable, no GUI)
  Either needs the user's go-ahead (multi-GB system install). A physical
  Android phone with USB debugging also works and skips the emulator.

[NEXT] Step 3 — First-run data path (software-only, unblocked).
  3a. Extract the seeder's generation logic into packages/core-db (adapter-
      based, bundle-safe) so scripts/seed-db.ts and the app share one
      implementation; Node seeder's determinism + physiology checks must
      still pass.
  3b. Store action loadDemoAthlete() for the empty state ("LOAD DEMO
      ATHLETE" button on ReadinessScreen's NO STATE VECTOR view).
  3c. Boot-time catch-up: materialize state_vector for the trailing week on
      every boot (idempotent) so today's row always exists once data flows.
  Verify: verify:all + re-run seeder assertions + fresh in-memory app-path
  simulation (migrate -> demo load -> today's row exists).

[BLOCKED on 2b] Step 4 — On-device smoke test.
  Log sets -> end session -> triggers update mech_daily -> state vector
  rematerializes -> readiness changes. Also: cold-start time, screen-reader
  pass on real hardware.

## Phase 7 — Subjective triage on device

[READY — Node-verifiable now] Step 5 — Device embedder adapter.
  onnxruntime-react-native + bundled int8 MiniLM (~23 MB) + pure-TS
  WordPiece tokenizer. Verify on THIS machine first: run the same ONNX via
  onnxruntime-node and assert vectors match the @xenova/transformers ones in
  assets/phrase-codebase.vectors.json (cosine > 0.999 per phrase).

[READY] Step 6 — REPORT UI on COACH tab.
  Text input -> matched cue + follow-up + guardrail-adjusted vector with
  amber GUARDRAIL badge; full-screen red directive on halt matches.
  NOTE (from step 2 bundle check): phrase-codebase.json is not yet imported
  by the app — wiring it in is part of this step; re-verify it appears in
  the Metro bundle afterwards.

[READY] Step 7 — Persist reports: 005_subjective_report.sql migration
  (date, raw text, matched entry id, similarity, action taken) + extend
  verify:db. Gives pain history longitudinal value.

## Phase 8 — Real telemetry (retire the seed data)

[ ] Step 8 — packages/biometrics: Health Connect (Android) -> hrv_daily /
    sleep_daily / spo2_sample; compaction job (fold + 14-day trim +
    materialize) wired to app foreground. HealthKit when iOS exists.
[ ] Step 9 — Movement library add/edit screen (movement table currently
    only populated by the seeder/demo loader).

## Phase 9 — Ship it free

[ ] Step 10 — CI: GitHub Actions running verify:all on push (needs the
    GitHub remote from Step 1's open decision; free on public repos).
    Note: CI can also run the Metro bundle check + a Gradle assembleDebug
    on GitHub's runners — which have the Android SDK preinstalled, so CI
    becomes the build machine even before the local toolchain exists.
[ ] Step 11 — Distribution: signed APK on GitHub Releases + F-Droid
    submission (all-local privacy story is F-Droid-friendly). iOS deferred
    (Mac + USD 99/yr).

## Backlog (valuable, not blocking)
History/trends screen; per-movement e1RM tracking; data export (CSV/SQLite
copy); voice input for reports; micro-cycle planner UI; localization.

## Standing facts
- verify:all = typecheck + db + policy + semantic + store + memory gates.
- Editing the phrase codebase requires `npm run embed:codebase`.
- npm scripts: seed, bench:cosine, embed:codebase; apps/mobile has
  bundle:android.
- The generative SLM stays removed; verify:memory fails the build if
  llama.rn reappears in any manifest.
