# Handover — 2026-07-13 · P16 S2–S4 shipped overnight (autonomous session)
Fable 5 (Cowork session) · Governing doc: `PHASE_PLAN_rev4_FORGE.md` · Ledger entries 0020–0025

## What shipped (UNCOMMITTED — review then commit; nothing pushed)

**S2 — import (run here; sandbox CAN reach github.com via clone, raw CDN blocked):**
432 staged / 441 quarantined from free-exercise-db (~870 ✓), Unlicense vendored,
KEEP-OURS collisions correct (8). Reasons audited: 240 out-of-scope category,
229 unmapped equipment (machine 65 / other 40), 61 no-pattern-only (worklist).

**S3 — curation, 53/434 records now curated (house voice per your review):**
- Batch 01 (15 staples) — committed by you already, then voice-revised.
- Batch 02 (15) + your 3 inline corrections applied (shrug path, elbow cue, curl rest position).
- Batch 03 (15): Decline Bench, DB Flye, Bench Dip, T-Bar Row, Chest-Supported DB Row,
  Deficit DL, Zercher, DB Sumo Squat, DB Reverse Lunge, DB Split Squat, Reverse Crunch,
  Hammer Curl, DB Calf Raise, Band External Rotation, KB Turkish Get-Up.
- Voice pass on the 16 most handout-flavored records (fault giveaways, load honesty).
- Progression rungs: Feet-Elevated Push-Up, Scapular Pull-Up curated; Pike Push-Up +
  Eccentric Wall HSPU house-authored (source:'house').
- Variants ticket (your open decision — resolved conservatively, reversible):
  4 records curated as prefix representations (see S4), 6 true dupes left
  UNCURATED so they can never seed (Barbell Deadlift, Barbell Squat, Front
  Barbell Squat, Barbell Shoulder Press, Band Pull Apart, Bodyweight Walking Lunge).

**Progression engine (your HSPU goal question, answered in code):**
- `packages/inference/src/progressionEngine.ts` — pure deterministic rung resolver;
  3×8-strict advancement (policy overridable); skipped rungs can't unlock; malformed
  chains throw. verify:progression = 14th gate (15 checks).
- Two chains seeded end-to-end (ranks 0–4):
  handstand-push-up: Push-up → Feet-Elevated → Pike → Eccentric Wall → HSPU
  pull-up: Lat Pulldown → Inverted Row → Scapular Pull-Up → Pull-Up → Weighted Pull-up

**S4 — seed migration + gate:**
- `scripts/generate-library-migration.mjs` (codegen; every mapping is reviewable data)
  → `016_movement_library_seed.sql` (GENERATED — regenerate, don't hand-edit).
- 49 rows seeded (movement + detail + taxonomy + equipment + progression side-car).
  Library: 30 → 79 movements. Idempotent (applies twice clean).
- 4 curated variants are NOT rows (010 no-implement-row-duplication rule): 2 already
  covered by shipped prefix sets; 2 prefix widenings — Overhead Press +Cable,
  Bodyweight Squat +DB.
- `movement_equipment` seeded (56 rows) — "no rows = bodyweight" is load-bearing.
  PROXIES to sanity-check: 'bench' = box/step platform; 'pullup_bar' = dip station.
- verify:library = 15th gate (18 checks, REAL-free so it passes old libsqlite).

**Tier gating (S4) — engine + store, pickers UI deferred (verifiable core first):**
- blockGenerator: beginner cap = Intermediate; untagged rows stay eligible
  (legacy byte-identical — machine-checked); a pattern with zero in-tier
  candidates falls back to the equipment pool (no holes).
- useStore passes movement_detail.difficulty_rating into the generator.
- 4 new checks in verify:blocks.

## Gates: 13/15 green locally
typecheck, demo, migrations, policy, blocks(+4), autopilot, biometrics, store,
coach, memory, progression(new), library(new) PASS.
verify:db = known sandbox libsqlite 3.37.2 STRICT/REAL quirk (expect green on CI).
verify:semantic / verify:embedder = CI-only (HF fetch blocked here).
verify:all now runs 15 gates. **Next push = first 15/15 CI run.**

## Judgment calls made overnight (review these first)
1. Variants policy: prefix-representation over new rows; true dupes excluded. Reversible
   by curating them later.
2. Pull-Up seeded as its OWN row despite "Bodyweight + Weighted Pull-up" being
   representable — progression rungs 3 vs 4 need distinct movements to log against.
   Tension with the 010 rule documented here deliberately.
3. Tier cap mapping: only 'beginner' is capped (≤ Intermediate). Intermediate+
   athletes see everything.
4. KB Turkish Get-Up classified core/rotation (not squat, despite dataset).
5. Equipment proxies (bench=box, pullup_bar=dip station) — vocabulary has no
   dedicated tokens; extend the 007 CHECK in a future migration if you want precision.
6. Progression advancement default = 3×8 strict in one session.

## Your checklist
1. Review the diff (5 modified, 3 new files + this handover). Read a few batch-03
   records to check the voice against your standard.
2. Commit, push → confirm CI 15/15 + APK artifact.
3. On-device (after existing P15 walk): fresh beginner athlete → generate block →
   confirm no Advanced movements prescribed; library shows 79 movements; your
   existing data upgrade path boots clean (016 is INSERT OR IGNORE only).
4. Sanity-check 016 spot content: `SELECT COUNT(*) FROM movement;` → 79.

## What's left (P16 → P19)
- S3 continues: 381 uncurated staged records (~15/ticket, FORGE loop or per-agent).
  61 no-pattern-only quarantines are the next-highest-value worklist.
- Classifier misfiles (e.g. Barbell Rollout staged as push) — fix per-record in curation.
- Progression UI: goal-movement selection (onboarding/ATHLETE tab) + rung display in
  session runner; more chains (pistol squat, dip, muscle-up?) are cheap now.
- Pickers: tier-filtered movement dropdowns (UI part of S4's mandate, deferred).
- P17 guided session runner (cues now exist for 53 movements). P18 rewards.
- P19: open Play Console + closed testing EARLY (12 testers × 14 days).

## Tooling status (worse than yesterday — important)
The file bridge corrupted Edit-tool writes to SESSION-CREATED files this session
(verify_library.py, generate-library-migration.mjs) — not just pre-existing ones.
Rule tightened: in Cowork, ALL repo writes go through bash (heredoc/python).
Also: run sandbox git with GIT_OPTIONAL_LOCKS=0 (a stale index.lock from a plain
`git status` blocked your commit earlier; deletable safely when no git runs).

---

## ADDENDUM — external audit response (GPT 5.6, 2026-07-13)

Status upgrade: S4 was correctly assessed as PARTIAL at audit time. All P1s and
F4/F6/F7 are now resolved in code; disposition below.

| # | Finding | Disposition |
|---|---------|-------------|
| F1 | Beginner fallback could prescribe Advanced | **FIXED.** Fallback removed; tier cap is now a hard bound like equipment — pattern drops with `no tier-eligible movement` warning. Gate: all-Advanced library prescribes a beginner ZERO slots (was: full block). |
| F2 | No gating in pickers/substitution | **FIXED (engine+UI).** `SubstitutionInput.maxDifficulty` caps layers 1+2 (optional, legacy-identical); SessionScreen picker filters beginners to ≤ Intermediate. NOTE: the audit's "Beginner whitelist" is invented policy — cap remains ≤ Intermediate pending your call. Store does not yet pass `maxDifficulty` into its substitution call sites — wire when the substitution UI next moves (flagged, small). |
| F3 | Prefix widenings vs equipment contradiction | **FIXED.** Widenings reverted; all 4 variants seed as rows with their own equipment (Cable Shoulder Press→cable_machine, Dumbbell Squat→dumbbells, BB Glute Bridge/BB Walking Lunge→barbell). Documented deviation from the 010 prepend rationale (it predates equipment-aware variants). Library = 83 rows. Open decision: shipped prefix arrays (e.g. Walking Lunge still lists BB) now overlap the new rows — trim in a future migration? |
| F4 | 4 curated records' content discarded | **FIXED** by F3 (they are rows; gate asserts their content). |
| F5 | verify:library weaker than mandate | **STRENGTHENED** to 27 checks: 1-3 cues + 2-5 instruction sentences per seeded row, banned medical/performance-claims regex, tier-visibility per category (all 8 covered, ≥2 beginner-visible per loaded category), variant-row assertions, shipped-content debt pinned at exactly 30. Picker/substitution visibility is TS-side and is checked in verify:blocks (7 tier checks). "Complete content across all 79/83" is intentionally NOT asserted: the shipped 30's empty text is documented 010 debt — pinned so drift is caught. |
| F6 | Codegen would overwrite shipped 016 | **GUARDED.** Regeneration now aborts if 016 exists with different content unless `AK_REGEN_016=1`; legal only until 016 ships. Post-ship curation requires a 017+ additive generator (small refactor, queued). |
| F7 | Progression isolated + same-session unprovable | **HALF-FIXED.** Engine contract reworked: history is per-session `repsPerSet` lists; 3×8 must occur within ONE session (gate: mixed sets, cross-session aggregation cases). Integration (store set_record→SessionSets query, generator/UI consumers) is explicitly labeled P17 scope in the engine header — it is machine-verified library code, not a live product path. |

Post-fix verification: typecheck + all 13 sandbox-runnable gates PASS
(verify:blocks 7 tier checks, verify:progression 17, verify:library 27).

---

## ADDENDUM 2 — re-audit response (all P1s closed, decisions ratified)

- **R1 (live substitution ungated): CLOSED.** The gate now rides the store's
  EXISTING `trainingAge` input — production is gated with the wiring the app
  already had, plus `beginnerOk` mapped through `toSubMovement`. verify:blocks
  greps the production sources for the wiring (crude but machine-checked; real
  RN component tests remain P17 debt).
- **R2 (Layer-3 triage): CLOSED.** `beginnerBars` applies in L1, L2, and the
  triage pool. Gate: Advanced accessories never enter a beginner's cluster.
- **R3 (whitelist): CLOSED — I was wrong, the plan mandates it** (P16 S4:
  "Beginner + whitelisted Intermediate staples"). Shipped as
  `movement_beginner_whitelist` (8 staples, Francis-ratified, no barbell:
  DB Bench, DB Shoulder Press, Incline DB Press, DB Step-Up, DB Split Squat,
  Cable Shoulder Press, Straight-Arm Pulldown, Cable Crunch). RDL was dropped
  by the rule itself (barbell lift) — caught by the gate. The `maxDifficulty`
  stopgap was removed; one mechanism only.
- **R4 (duplicate identity): DECIDED by Francis** — BB Glute Bridge/BB Walking
  Lunge dropped as rows (prefix-encoded, no split history); equipment-distinct
  variants stay rows. Library = **81 rows**. Recorded in DEVIATION_LOG.md.
- **R5 (gate gaps): CLOSED where DB-checkable** — 2-4 steps (was 2-5; Hip
  Thrust trimmed), sentence split on [.!?], banned-claims regex broadened
  (insurance/pays out/bulletproof/prevent-injury/pain-free; Band External
  Rotation reworded), tier-visibility counts derived from movement.pattern
  exactly as the picker derives them. Picker/store wiring checked by source
  grep in verify:blocks.
- **R6: acknowledged, unchanged** — 017+ additive generator before the next
  curation wave ships; progression store/UI integration is P17 (labeled).

Post-fix: typecheck + all 13 sandbox-runnable gates green
(verify:blocks 13 tier/production checks, verify:library 29, verify:progression 17).
